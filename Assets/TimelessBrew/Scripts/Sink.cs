namespace TimelessBrew
{
    /// <summary>
    /// Раковина (§3.3, §6.7): кран холодной воды и место просеивания. Клик сосудом, который может
    /// принять воду (чайник, турка с молотым) — набрать; удержание дуршлагом с зёрнами — трясти,
    /// шелуха улетает в слив (§4.2, этап 2). Это маркер-цель, вся логика — в <see cref="Hand"/>.
    /// </summary>
    public class Sink : UnityEngine.MonoBehaviour { }
}
